import 'colors'
import Assert from '../../lib/assert'

/**
 * Manipulation des chaines de caractères
 */

// Convertir une chaine de caractères en majuscule, 
// sans utiliser .toLowerCase, en utilisant une boucle

const toUpperCase = (string) => {
    
}

console.log('Test: to uppercase string (enhanced)'.blue)
Assert.equals(addString('ma Chaine de caractères 12'), 'MA CHAINE DE CARACTÈRES 12')